# ---------------------------------------------------------------
# kora-native — что НЕ попадает в репозиторий
# ---------------------------------------------------------------

# Архив другого проекта (pixelhomie), лежит здесь только как доставка
pixelhomie.zip

# Корзина: файлы, помеченные к удалению, но физически ещё не стёртые
_to_delete/

# Зависимости — ставятся заново, в истории им не место
node_modules/

# Локальные проверочные прогоны и логи
*.log
*.tmp
/screenshots/
/tmp/

# Мусор Windows / macOS
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store

# Настройки редакторов
.vscode/
.idea/
